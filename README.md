# 绍派伤寒 · 学术知识图谱

> **沈钦荣名中医 × 医哲未来人工智能研究院 联合研发**
> Jointly developed by Dr. Shen Qinrong & IMPFAI

绍派伤寒（Shaoxing Cold-Damage School）学术知识图谱的网页版。发端于明代张景岳，
形成于清代俞根初《通俗伤寒论》，光大于何廉臣——以图谱之形重现越中外感病学派的
医家、著作、学说与四诊征象。

**当前版本 V2**：3,951 节点 / 2,645 关系，含谱系层与诊法层。

## 在线访问

- 主页：<https://psknlr.github.io/Shaopai-Shanghan/>
- 知识图谱：<https://psknlr.github.io/Shaopai-Shanghan/explorer.html>

> **网址区分大小写**：仓库名为 `Shaopai-Shanghan`，两个 `S` 必须大写。
> 输入 `shaopai-shanghan`（全小写）会返回 404。
>
> `psknlr.github.io` 根地址已被另一站点占用，本项目发布在 `/Shaopai-Shanghan/` 子路径下。

## 站点结构

| 文件 | 说明 |
| --- | --- |
| `index.html` | 主页。源流、学术特色、代表医家、诊法层、图谱预览、数据下载与联合研发说明 |
| `explorer.html` | 知识图谱浏览器。3,951 节点 / 2,645 关系，含全部数据（自包含单文件，约 2.5 MB，gzip 后约 460 KB） |
| `data/` | 机器可读数据集，见下 |
| `404.html` | 自定义 404 页，样式与站点一致 |
| `shaopai_explorer.html` | 旧文件名，自动跳转至 `explorer.html` |
| `.nojekyll` | 仅在「从分支发布」模式下生效；当前用 Actions 发布，保留以备切换 |
| `.github/workflows/jekyll-gh-pages.yml` | Pages 部署流水线 |

网页部分为**零依赖单文件**：不加载任何外部 CDN、字体或脚本，
可直接双击在本地浏览器打开，也可整体拷贝离线使用。

## 图谱内容

**节点 3,951**

| 类别 | 数量 | 层 |
| --- | ---: | --- |
| 学术观点 Doctrine | 1,524 | 谱系层 |
| 诊法征象 DiagnosticSign | 1,311 | 诊法层 |
| 证候 Pattern | 542 | 诊法层 |
| 著作 Work | 356 | 谱系层 |
| 医家 Physician | 218 | 谱系层 |

**关系 2,645**：主张 1,142 · 征象提示 605 · 子证候 272 · 撰写 223 · 载于 135 ·
私淑 81 · 校勘增订 75 · 属于六经 59 · 征象排除 35 · 师承 18

**诊法征象按模态**：问诊 412 · 舌诊 265 · 望诊 260 · 脉诊 168 · 腹诊 110 · 目诊 75 · 闻诊 21

图谱呈**双星结构**：谱系层（1,444 节点）与诊法层（663 节点）各成一瓣，
目前互不相连——原文未把诊法征象直接系于提出它的医家。详见「构建状态」。

## 功能

**主页**

- 江南水乡首屏场景（会稽远山、白墙黛瓦、石拱桥、鉴湖水波、乌篷船），支持鼠标视差
- 学派源流时间轴：明代张景岳 → 清代俞根初 → 何秀山 / 章虚谷 / 赵晴初 →
  何廉臣 → 曹炳章 / 胡宝书 → 徐荣斋 → 当代整理与智能化
- 学术特色六端，每条均附原书文句与出处医家
- 代表医家十二家卡片，点击直达图谱对应节点
- 诊法层：征象模态分布、证候的六经与性质坐标
- 图谱实时预览（取真实布局坐标绘制，双瓣结构）
- 机器可读数据集下载

**知识图谱**

- 中文检索医家 / 著作 / 观点 / 征象 / 证候，支持异名与观点全文匹配
- 按节点类型、关系类型与**诊法模态**（舌·脉·腹·目）三重筛选
- 点选节点查看关系网与逐条原文证据、章节路径、段落编号
- 关系可点击跳转，支持顺藤摸瓜式溯源
- 深链接：`explorer.html?q=腹诊` 直接定位到指定节点
- 桌面端鼠标拖拽 / 滚轮缩放，移动端单指平移 / 双指缩放
- 键盘快捷键：`/` 搜索、`F` 全景、`T` 切换主题、`Esc` 关闭、`?` 帮助

**双主题**：宣纸（浅）/ 墨夜（深），跨页面记忆，默认跟随系统。

## 数据集

`data/` 下为机器可读导出，所有关系均带出处字段
（`source_sentence` · `chapter_path` · `passage_id` · `evidence_verbatim` · `engine` · `agreement`）。

| 文件 | 说明 |
| --- | --- |
| `shaopai_kg.json` | 主数据集，含本体、受控词表、全部节点与关系及**完整**出处 |
| `nodes.csv` / `edges.csv` | Neo4j 导入格式，含布局坐标与连通分量 |
| `shaopai_instances.ttl` | RDF A-Box，以 `rdf:Statement` 具体化承载出处 |
| `layout_positions.json` | 全部 3,951 个节点的布局坐标，保证渲染可复现 |
| `load_neo4j.cypher` | 约束、索引与分批 `LOAD CSV` |
| `example_queries.cypher` | 六个可直接运行的图遍历示例 |
| `QC_AUDIT.md` / `qc_audit.json` | 质检报告 |
| `pilot_validation.json` | 试抽取评分及其未测项 |

Neo4j 导入：把 `nodes.csv`、`edges.csv` 放入 DBMS 的 `import/` 目录后

```bash
cat data/load_neo4j.cypher | cypher-shell -u neo4j -p <password>
```

RDF 查询：Turtle 中所有中文字面量带 `@zh` 语言标记，SPARQL 需写 `"腹诊"@zh`，
写成 `"腹诊"` 会静默返回空结果。

> 页面内嵌的图谱为**精简副本**：每个节点最多保留 8 条原文出处以控制体积，
> 完整出处请用 `data/shaopai_kg.json`。

## 构建状态与限制

本体 1.0.0 定义 13 类 / 26 关系；四个抽取层中**已完成两层**：

| 层 | 状态 | 段落 |
| --- | --- | ---: |
| 谱系层（医家·著作·学术观点） | 完成 | 407 |
| 诊法层（舌·脉·腹·目诊） | 完成 | 281 / 284 |
| 证候层（病证·证候·症状·治法） | 未运行 | 786 |
| 本草方剂层（方剂·中药·加减·医案） | 未运行 | 555 |

**限制**（引用 `data/QC_AUDIT.md`）：

1. **两层互不相连**——原文没有把诊法征象或证候系回描述它的医家，本体中也没有相应关系可用。
   这是真实的数据缺口，不是布局假象；需新增 `describedBy` 关系或等证候层补齐。
2. **全部关系单模型抽取**，`agreement` 一律为 `single_engine`，交叉验证未运行。
3. **96.5%** 的关系有逐字原文证据，其余标注 `evidence_verbatim=false`，请回溯原书核验。
4. **96.4%** 的关系仅有一处文献支撑；1,149 项诊法征象只出现一次。
5. **796 项征象未抽出「提示何种证候」的断言**，在图谱中呈孤点。
6. 证候节点由诊法关系派生，其六经与性质由**名称**经词表匹配推得——
   某证候未标六经，只说明其名称未含六经字样，不等于原文认为它不属某经。
7. 356 部著作中有 11 条实为参考文献中的期刊论文题名。
8. 籍贯粒度不一（山阴 / 绍兴），张畹香记为「江南」。

本体校验：定义域/值域违例 0 / 2,645，悬空端点 0，自环 3。

> 上游 V2 包的 README 还列出了 `shaopai_ontology.ttl`（T-Box）、`corpus_shaopai.json`、
> `ontology_spec.json`、`vocabularies.json`、`kg_build.py` 等文件，但压缩包内并未包含，
> 故未收入 `data/`。本体与受控词表的内容可从 `shaopai_kg.json` 的 `ontology`、
> `vocabularies` 两个字段取得。

## 部署

站点通过 GitHub Actions 发布，配置见 `.github/workflows/jekyll-gh-pages.yml`：

- **Settings → Pages → Source** 为 `GitHub Actions`
- 每次推送到 `main` 自动触发构建与部署，也可在 **Actions** 页手动运行
- 从推送到线上可见约需 1–2 分钟；期间访问会短暂出现 404，属正常现象

页面均无 YAML front matter，Jekyll 将其作为静态文件原样拷贝，不做 Liquid 解析，
因此页面内的 `{` `}` 等字符不会被改写。

> 若日后新增页面，避免在文件开头写 `---` 分隔的 front matter，也避免使用以
> `_` 开头的文件名或目录——两者都会被 Jekyll 特殊处理。

## 设计语汇

取绍兴地方文化意象：宣纸底色、松烟墨字、越窑青瓷绿、绍兴黄酒琥珀、朱砂印章，
配以白墙黛瓦、石拱桥、乌篷船与鉴湖水波。五类节点分取朱砂（医家）、天青（著作）、
青瓷（学术观点）、缃色（诊法征象）、藕荷（证候）。中文标题用楷体，正文用宋体，
数字与标签用无衬线体，字体全部取自系统，无外链。

## 数据来源

- **学术底本**：《浙派中医丛书》专题系列·绍派伤寒，沈钦荣 主编，2021 年。
  语料 205,712 字，1,243 个段落，11 位医家。
- **图谱构建**：医哲未来人工智能研究院（IMPFAI）· <https://impfai.github.io/>
- 原书文字著作权归原作者与出版方所有；本图谱为衍生研究数据集，不对原始材料主张任何权利。

本站仅供学术研究与文献检索之用，**不作为临床诊疗依据**。
