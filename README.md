# 绍派伤寒 · 学术知识图谱

> **沈钦荣名中医 × 医哲未来人工智能研究院 联合研发**
> Jointly developed by Dr. Shen Qinrong & IMPFAI

绍派伤寒（Shaoxing Cold-Damage School）学术知识图谱的网页版。发端于明代张景岳，
形成于清代俞根初《通俗伤寒论》，光大于何廉臣——以图谱之形重现越中外感病学派的
医家、著作、学说、四诊征象与方药配伍。

**当前版本 V3**：4,205 节点 / 3,034 关系，含谱系层、诊法层与本草方剂层。

## 在线访问

- 主页：<https://psknlr.github.io/Shaopai-Shanghan/>
- 知识图谱：<https://psknlr.github.io/Shaopai-Shanghan/explorer.html>

> **网址区分大小写**：仓库名为 `Shaopai-Shanghan`，两个 `S` 必须大写。
> 输入 `shaopai-shanghan`（全小写）会返回 404。
>
> `psknlr.github.io` 根地址已被另一站点占用，本项目发布在 `/Shaopai-Shanghan/` 子路径下。

## 站点结构

| 文件 | 说明 |
| --- | --- |
| `index.html` | 主页。源流、学术特色、代表医家、诊法层、方药层、图谱预览、数据下载与联合研发说明 |
| `explorer.html` | 知识图谱浏览器。4,205 节点 / 3,034 关系，含全部数据（自包含单文件，约 2.9 MB） |
| `data/` | 机器可读数据集，见下 |
| `404.html` | 自定义 404 页，样式与站点一致 |
| `shaopai_explorer.html` | 旧文件名，自动跳转至 `explorer.html` |
| `.nojekyll` | 仅在「从分支发布」模式下生效；当前用 Actions 发布，保留以备切换 |
| `.github/workflows/jekyll-gh-pages.yml` | Pages 部署流水线 |

网页部分为**零依赖单文件**：不加载任何外部 CDN、字体或脚本，
可直接双击在本地浏览器打开，也可整体拷贝离线使用。

## 图谱内容

**节点 4,205**

| 类别 | 数量 | 层 |
| --- | ---: | --- |
| 学术观点 Doctrine | 1,524 | 谱系层 |
| 诊法征象 DiagnosticSign | 1,311 | 诊法层 |
| 证候 Pattern | 542 | 诊法层 |
| 著作 Work | 356 | 谱系层 |
| 医家 Physician | 218 | 谱系层 |
| 中药 Herb | 188 | 本草方剂层 |
| 方剂 Formula | 41 | 本草方剂层 |
| 医案 CaseRecord | 15 | 本草方剂层 |
| 治法 TreatmentPrinciple | 10 | 本草方剂层 |

**关系 3,034**：主张 1,142 · 征象提示 605 · 子证候 272 · 药物组成 262 · 撰写 223 ·
载于 135 · 私淑 81 · 校勘增订 75 · 属于六经 59 · 君臣佐使 41 · 征象排除 35 ·
源自 27 · 随症加药 22 · 师承 18 · 医案医家 15 · 治法用方 10 · 随症减药 7 · 方剂主治 5

**诊法征象按模态**：问诊 412 · 舌诊 265 · 望诊 260 · 脉诊 168 · 腹诊 110 · 目诊 75 · 闻诊 21

**方剂按医家**：张景岳 13 · 俞根初 11 · 曹炳章 8 · 何廉臣 6 · 胡宝书 2 · 赵晴初 1

图谱是**单一巨分量 2,355 节点**。上一版中谱系层与诊法层互不相连，本草方剂层同时
连向两侧（`derivesFrom`→著作、`formulaTreats`→证候、`caseByPhysician`→医家），
把两瓣接通了。

## 功能

**主页**

- 江南水乡首屏场景（会稽远山、白墙黛瓦、石拱桥、鉴湖水波、乌篷船），支持鼠标视差
- 学派源流时间轴：明代张景岳 → 清代俞根初 → 何秀山 / 章虚谷 / 赵晴初 →
  何廉臣 → 曹炳章 / 胡宝书 → 徐荣斋 → 当代整理与智能化
- 学术特色六端，每条均附原书文句与出处医家
- 代表医家十二家卡片，点击直达图谱对应节点
- 诊法层：征象模态分布、证候的六经与性质坐标
- 方药层：方剂配伍卡（含君臣佐使与原剂量）、剂量分布、连通性改善说明
- 图谱实时预览（取真实布局坐标绘制）
- 机器可读数据集下载

**知识图谱**

- 中文检索医家 / 著作 / 观点 / 征象 / 证候，支持异名与观点全文匹配
- 按节点类型（九类）、关系类型与**诊法模态**（舌·脉·腹·目）三重筛选
- 方剂节点展示逐味药的**原剂量、炮制与君臣佐使**，加减关系附触发条件
- 点选节点查看关系网与逐条原文证据、章节路径、段落编号
- 关系可点击跳转，支持顺藤摸瓜式溯源
- 深链接：`explorer.html?q=腹诊` 直接定位到指定节点
- 桌面端鼠标拖拽 / 滚轮缩放，移动端单指平移 / 双指缩放
- 键盘快捷键：`/` 搜索、`F` 全景、`T` 切换主题、`Esc` 关闭、`?` 帮助

**双主题**：宣纸（浅）/ 墨夜（深），跨页面记忆，默认跟随系统。

## 数据集

`data/` 下为机器可读导出，所有关系均带出处字段
（`source_sentence` · `chapter_path` · `passage_id` · `evidence_verbatim` · `engine` · `agreement`）。

| 文件 | 说明 |
| --- | --- |
| `shaopai_kg.json` | 主数据集，含本体、受控词表、全部节点与关系及**完整**出处 |
| `corpus_shaopai.json` | 自原书复原的分段语料（1,586 段），带章节路径 |
| `nodes.csv` / `edges.csv` | Neo4j 导入格式，含布局坐标与连通分量 |
| `shaopai_instances.ttl` | RDF A-Box，以 `rdf:Statement` 具体化承载出处 |
| `layout_positions.json` | 全部 4,205 个节点的布局坐标，保证渲染可复现 |
| `load_neo4j.cypher` | 约束、索引与分批 `LOAD CSV` |
| `example_queries.cypher` | 六个可直接运行的图遍历示例 |
| `QC_AUDIT.md` / `qc_audit.json` | 质检报告 |
| `pilot_validation.json` | 试抽取评分及其未测项 |

Neo4j 导入：把 `nodes.csv`、`edges.csv` 放入 DBMS 的 `import/` 目录后

```bash
cat data/load_neo4j.cypher | cypher-shell -u neo4j -p <password>
```

RDF 查询：Turtle 中所有中文字面量带 `@zh` 语言标记，SPARQL 需写 `"腹诊"@zh`，
写成 `"腹诊"` 会静默返回空结果。

> 页面内嵌的图谱为**精简副本**：每个节点最多保留 8 条原文出处以控制体积，
> 完整出处请用 `data/shaopai_kg.json`。

## 构建状态与限制

本体 1.0.0 定义 13 类 / 26 关系；四个抽取层中**已完成三层**：

| 层 | 状态 | 抽取方式 |
| --- | --- | --- |
| 谱系层（医家·著作·学术观点） | 完成 | 单模型 LLM |
| 诊法层（舌·脉·腹·目诊） | 完成 | 单模型 LLM |
| 本草方剂层（方剂·中药·治法·医案） | 完成 | **确定性解析器** |
| 证候层（病证·证候·症状） | 未运行 | — |

本草方剂层不由语言模型抽取，而是对原书「方剂选录」「医案选按」等结构规整的章节
做确定性解析（`tools/`下可复跑），**同一输入必得同一图谱**。抽样校验：蒿芩清胆汤
8/8 味全中，与上游 `pilot_validation.json` 的金标准一致。

**限制**（详见 `data/QC_AUDIT.md`）：

1. 谱系层与诊法层的关系为**单模型抽取**，`agreement` 为 `single_engine`，交叉验证未运行。
2. **96.9%** 的关系有逐字原文证据，其余标注 `evidence_verbatim=false`。
3. **96.8%** 的关系仅有一处文献支撑；1,149 项诊法征象只出现一次。
4. **药名未作归一**：生枳壳与枳壳、炙甘草与甘草各自成点。剥离前缀的方案已被否决——
   它会把陈皮 / 青皮 / 新会皮一并压成「皮」。
5. 只有原书**印出配伍**的方剂才立为节点；仅被提及方名者（麻黄汤、白虎汤等）不立，
   以免产生孤点。
6. `Dosage` 类按设计保持 0 实例：剂量、炮制与君臣佐使挂在 `hasIngredient` /
   `ingredientRole` 关系上，而非另立节点。
7. `formulaTreats` 依赖主治文字与既有证候名的字符串包含匹配，仅命中 5 条。
8. 796 项诊法征象未抽出所提示的证候，呈孤点。
9. 本草层的段落编号为 `b#####`，来自本次对原书 .doc 的重新切分，
   **与谱系层、诊法层的 `p#####` 不同源**，不可直接对齐；核验以逐字原文与章节路径为准。
10. 356 部著作中有 11 条实为参考文献中的期刊论文题名。

本体校验：定义域/值域违例 0 / 3,034，悬空端点 0，全部关系带出处。

## 重建流程

```bash
python3 tools/build_materia_kg.py --write   # 抽取本草层并并入 shaopai_kg.json
python3 tools/export_csv.py                 # 重建 nodes.csv / edges.csv
python3 tools/export_rdf.py                 # 重建 shaopai_instances.ttl
python3 tools/build_payload.py              # 重建两个页面的内嵌数据与布局
```

新增节点的坐标用「锁定既有节点、只松弛新节点」的力导向布局求解，
既有坐标一字不动——上游承诺每次渲染可复现，重排全图会毁掉这个性质。

## 部署

站点通过 GitHub Actions 发布，配置见 `.github/workflows/jekyll-gh-pages.yml`：

- **Settings → Pages → Source** 为 `GitHub Actions`
- 每次推送到 `main` 自动触发构建与部署，也可在 **Actions** 页手动运行
- 从推送到线上可见约需 1–2 分钟；期间访问会短暂出现 404，属正常现象

页面均无 YAML front matter，Jekyll 将其作为静态文件原样拷贝，不做 Liquid 解析，
因此页面内的 `{` `}` 等字符不会被改写。

> 若日后新增页面，避免在文件开头写 `---` 分隔的 front matter，也避免使用以
> `_` 开头的文件名或目录——两者都会被 Jekyll 特殊处理。

## 设计语汇

取绍兴地方文化意象：宣纸底色、松烟墨字、越窑青瓷绿、绍兴黄酒琥珀、朱砂印章，
配以白墙黛瓦、石拱桥、乌篷船与鉴湖水波。九类节点分取朱砂（医家）、天青（著作）、
青瓷（学术观点）、缃色（诊法征象）、藕荷（证候）、胭脂（方剂）、柳芽（中药）、
石青（治法）、月白（医案）。中文标题用楷体，正文用宋体，
数字与标签用无衬线体，字体全部取自系统，无外链。

## 数据来源

- **学术底本**：《浙派中医丛书》专题系列·绍派伤寒，沈钦荣 主编，2021 年。
  语料自原书 Word 文档复原：1,586 个段落、203,045 字（上游审计口径 205,712，差 1.3%），11 位医家。
- **图谱构建**：医哲未来人工智能研究院（IMPFAI）· <https://impfai.github.io/>
- 原书文字著作权归原作者与出版方所有；本图谱为衍生研究数据集，不对原始材料主张任何权利。

本站仅供学术研究与文献检索之用，**不作为临床诊疗依据**。
